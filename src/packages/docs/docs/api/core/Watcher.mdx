---
sidebar_position: 18
---

import { ApiLink } from '@site/src/components/api-link'
import { SimpleConsolePlayground } from '@site/src/components/live-playground'
import { MarkdownWrapper } from '@site/src/components/markdown-wrapper'
import { ObjectShapeDefinition } from '@site/src/components/object-shape-definition'
import { DataType, TypeDisplay } from '@site/src/components/type-display'
import { DocConstants } from '@site/src/constants'

# Watcher

## Overview

<TypeDisplay>{DataType.class}</TypeDisplay>

<MarkdownWrapper>{DocConstants.TSDOC_DESC_WATCHER}</MarkdownWrapper>

```ts
class WatcherWatcher<Args extends any[]> { }
```

### Constructor
```ts
constructor()
```

#### Parameters
The constructor does not take any parameters.

#### Example
```js
import { Watcher } from '{:CORE_PACKAGE_NAME:}'

const watcher = new Watcher()
const unwatch = watcher.watch(() => { ... })
watcher.refresh(...) // Arguments can be passed
unwatch()
```

## Properties

There are no publicly available properties.

## Methods

### watch
<MarkdownWrapper>{DocConstants.TSDOC_METHOD_DESC_WATCHER_WATCH}</MarkdownWrapper>
```ts
watch(callback: ((...args: Args) => void)): (() => void)
```

### refresh
<MarkdownWrapper>{DocConstants.TSDOC_METHOD_DESC_WATCHER_REFRESH}</MarkdownWrapper>
```ts
refresh(...args: Args): void
```

### unwatchAll
<MarkdownWrapper>{DocConstants.TSDOC_METHOD_DESC_WATCHER_UNWATCH_ALL}</MarkdownWrapper>
```ts
unwatchAll(): void
```

### dispose
<MarkdownWrapper>{DocConstants.TSDOC_METHOD_DESC_WATCHER_DISPOSE}</MarkdownWrapper>
```ts
dispose(): void
```
